class sample;
   rand logic [7:0] ass_arr [4:0];
   constraint cu{unique{ass_arr};}
endclass
module tb;
  sample s=new();
  initial begin
    repeat(5) begin
      assert(s.randomize());
      $display("arr=%0p",s.ass_arr);
    end
  end
endmodule
